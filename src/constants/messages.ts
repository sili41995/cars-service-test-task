const enum Messages {
  emptyList = 'Adverts list is empty',
  loadedSuccess = 'Adverts loaded successfully',
  fetchAdvertByIdError = 'Fetch advert failed',
  fetchAdvertsError = 'Fetch adverts failed',
  notFound = 'The request URL was not found',
}

export default Messages;
